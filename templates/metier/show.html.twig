{% extends 'base.html.twig' %}

{% block title %}Metier{% endblock %}

{% block body %}
    <h1>Metier</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ metier.id }}</td>
            </tr>
            <tr>
                <th>Libmetier</th>
                <td>{{ metier.libmetier }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('metier_index') }}" class="btn btn-sm btn-secondary"><i class="fas fa-backspace"></i></a>

    <a href="{{ path('metier_edit', {'id': metier.id}) }}" class="btn btn-sm btn-success"><i class="fas fa-edit"></i></a>

    {{ include('metier/_delete_form.html.twig') }}
{% endblock %}
